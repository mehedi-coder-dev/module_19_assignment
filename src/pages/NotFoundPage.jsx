import React from 'react';
import Layout from '../layout/Layout';
import NotFoundPages from '../components/NotFoundPage/NotFoundPages'

const NotFoundPage = () => {
    return (
        <Layout>
            <NotFoundPages/>
            
        </Layout>
    );
}

export default NotFoundPage;
